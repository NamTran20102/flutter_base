name: flutter_base
description: This is base flutter project.
version: 0.0.1
homepage:

environment:
  sdk: '>=3.0.5 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  path: ^1.8.3

dev_dependencies:
  flutter_test:
    sdk: flutter
